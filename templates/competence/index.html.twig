{% extends 'base.html.twig' %}

{% block title %}Compétences du Héros{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="h3 fw-bold">Mes Compétences</h1>
        <a href="{{ path('competence_new') }}" class="btn btn-outline-primary">
            ➕ Nouvelle compétence
        </a>
    </div>

    {% if competences is empty %}
        <div class="alert alert-secondary">Aucune compétence trouvée.</div>
    {% else %}
        <div class="table-responsive">
            <table class="table table-striped table-hover align-middle">
                <thead class="table-dark">
                    <tr>
                        <th>Nom</th>
                        <th>Description</th>
                        <th>Effet</th>
                        <th>Origine</th>
                        <th class="text-end">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {% for competence in competences %}
                        <tr>
                            <td>{{ competence.name }}</td>
                            <td>{{ competence.description|length > 50 ? competence.description|slice(0, 50) ~ '...' : competence.description }}</td>
                            <td>{{ competence.effet }}</td>
                            <td>{{ competence.origine }}</td>
                            <td class="text-end">
                                <a href="{{ path('competence_edit', {id: competence.id}) }}" 
                                   class="btn btn-sm btn-outline-secondary me-1">
                                    ✏️ Éditer
                                </a>
                                <form method="post" action="{{ path('competence_delete', {id: competence.id}) }}" 
                                      style="display:inline;" 
                                      onsubmit="return confirm('Voulez-vous vraiment supprimer cette compétence ?');">
                                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ competence.id) }}">
                                    <button class="btn btn-sm btn-outline-danger" type="submit">
                                        🗑 Supprimer
                                    </button>
                                </form>
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    {% endif %}
</div>
{% endblock %}
