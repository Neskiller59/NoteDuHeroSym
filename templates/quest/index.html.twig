{% extends 'base.html.twig' %}

{% block title %}Quêtes{% endblock %}

{% block body %}
<div class="container mt-4" style="max-width:900px;">

    <h1 class="mb-4">Quêtes de {{ hero.name }}</h1>

    {# Bouton Nouvelle Quête avec heroId #}
    <a href="{{ path('quest_new', { heroId: hero.id }) }}" class="btn btn-success mb-3">➕ Nouvelle Quête</a>

    {% for quest in quests|default([]) %}
        <div class="card mb-3 shadow-sm" style="background-color:#2a2a3f; color:#fff;">
            <div class="card-body d-flex justify-content-between align-items-start">
                <div>
                    <h3 class="card-title">{{ quest.name }}</h3>
                    <p class="mb-1"><strong>Objectif :</strong> {{ quest.objectif }}</p>
                    {% if quest.information %}
                        <p class="mb-1"><strong>Informations supplémentaires :</strong> {{ quest.information }}</p>
                    {% endif %}
                    {% if quest.origine %}
                        <p class="mb-1"><strong>Origine :</strong> {{ quest.origine }}</p>
                    {% endif %}
                </div>

                <div class="d-flex align-items-start" style="white-space: nowrap;">
                    {# Edit avec heroId #}
                    <a href="{{ path('quest_edit', {'heroId': hero.id, 'id': quest.id}) }}" class="btn btn-warning btn-sm me-2" title="Modifier">✏️</a>

                    {# Delete avec heroId #}
                    <form method="post" action="{{ path('quest_delete', {'heroId': hero.id, 'id': quest.id}) }}" style="display:inline;" onsubmit="return confirm('Supprimer cette quête ?');">
                        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ quest.id) }}">
                        <button type="submit" class="btn btn-danger btn-sm" title="Supprimer">🗑️</button>
                    </form>
                </div>
            </div>
        </div>
    {% else %}
        <p>Aucune quête disponible pour ce héros.</p>
    {% endfor %}

    {# Messages flash #}
    {% for label, messages in app.flashes %}
        {% for message in messages %}
            <div class="alert alert-{{ label }} alert-dismissible fade show mt-3" role="alert">
                {{ message }}
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        {% endfor %}
    {% endfor %}
</div>
{% endblock %}
